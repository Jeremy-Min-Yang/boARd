import SwiftUI
import PDFKit
import UIKit

// Enum definition for CourtType
enum CourtType {
    case full
    case half
}

// This file should be added to replace the current WhiteboardView.swift.
// It contains the correct imports to properly integrate with SavedPlayService.

// IMPORTANT: This is a clean import file.
// Copy the entire content from WhiteboardView.swift (except for the imports at the top)
// and add it below this comment.

// Then add the loadPlay function that accepts a SavedPlay to load saved plays. 